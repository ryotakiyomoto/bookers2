<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
    <%= render 'users/profile', user: @user %>
    <%= render 'books/form', book: @book_new %>
    <%= render 'books/bookinfo', book: @book_new %>
    </div>
  </div>
</div>
