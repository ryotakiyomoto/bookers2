  <div class="col-md-8 offset-md-1">
  <h2 class='mt-3'>Books</h2>
  <table class="table table-hover table-inverse">
    <thead>
      <th></th>
      <th>Title</th>
      <th>Opinion</th>
      <th colspan="3"></th>
    </thead>
    <tbody>
      <% @books.each do |book| %>
      <tr>
        <td>
          <%= link_to book.user do %>
        <div class='img-store'>
          <%= image_tag 'no_image.jpg' %>
        </div>
            <%= 
            # attachment_image_tag book.user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "/assets/no_image.jpg", class: 'attachment user profile_image fallback', size: "40x40"  
            %>
          <% end %>
        </td>
        <td><%= link_to book.title, book %></td>
        <td><%= book.body %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
<div>